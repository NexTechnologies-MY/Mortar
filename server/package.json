{
  "name": "server",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "bun --env-file=../.env --hot src/index.ts",
    "start": "bun src/index.ts",
    "db:reset": "bun --env-file=../.env db/cli.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun --env-file=../.env test src && bun --env-file=../.env test db"
  },
  "dependencies": {
    "@mortar/core": "workspace:*",
    "@mortar/jev": "workspace:*"
  },
  "devDependencies": {
    "bun-types": "^1.3.4",
    "typescript": "^5.8.3"
  }
}
